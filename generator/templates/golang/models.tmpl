{{- range .Models.Objects}}
{{$.GoDoc .Name .Description}}
type {{.Name}} struct {
	{{- range .Fields}}
		{{- if .Description}}
		{{$.GoDoc .Name .Description}}
		{{- end}}
		{{$.GoName .Name}} {{if not .NonNull}}*{{end}}{{.Type}}
	{{- end}}
}
{{end}}
